 
name: Main workflow
on: [push]
jobs:
  build:
    container: mcr.microsoft.com/dotnet/core/sdk:3.0
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - run: dotnet restore
    - run: dotnet build
